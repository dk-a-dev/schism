<?xml version="1.0" encoding="utf-8"?>
<!--
  The self-hosted backend is plain HTTP. Permit cleartext only to the local dev hosts:
  the Android emulator's host loopback (10.0.2.2) and localhost. Debug builds widen this
  (see src/debug/res/xml/network_security_config.xml) so a LAN IP backend also works.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
</network-security-config>
